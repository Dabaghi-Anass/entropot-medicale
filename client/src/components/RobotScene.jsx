import React, { useEffect, useState } from 'react'
import { initScene } from "../components/threeScene"
// import robotModel from
export default function RobotScene({ size }) {
  return (
    <div style={{
      width: "100%",
      height: "100%"
    }} id="Scene"></div>
  )
}
